import { Box } from '@chakra-ui/icons';
import { FC, memo } from 'react';

import Header from '~/components/header/Header';

export const MainPage: FC = memo(() => (
    <Box color='gray.50' className='MainPage'>
        <Header />
        kndsfnasfna
    </Box>
));
